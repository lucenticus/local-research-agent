mlx-lm>=0.28
FlagEmbedding>=1.3
langgraph>=1.2
langchain-core>=1.5
langchain-mcp-adapters>=0.3
mcp>=1.29
langsmith>=0.10
# scripts/eval_ragas.py's context precision/recall metrics — pin langchain-
# community <0.4: ragas 0.4.x's ragas/llms/base.py unconditionally imports
# langchain_community.chat_models.vertexai at module load, removed upstream
# in langchain-community 0.4 (confirmed: `import ragas` crashes otherwise).
ragas>=0.4.3,<0.5
langchain-community==0.3.30
# Docker-сервер (docker-compose.yml qdrant service), не embedded — см.
# store/qdrant_store.py.
qdrant-client>=1.19
numpy
beautifulsoup4
pymupdf
fastapi
uvicorn
httpx
python-dotenv
pytest
